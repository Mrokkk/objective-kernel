#!/bin/bash

$(which qemu-system-i386 || which qemu-system-x86_64) -kernel @CMAKE_BINARY_DIR@/@PROJECT_NAME@-tests -display none -serial stdio -no-reboot -cpu core2duo $@ | tee log
if grep -q "failed" log; then
    exit 1
fi

